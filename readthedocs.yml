# Source: http://docs.readthedocs.io/en/latest/yaml-config.html

# Don't build any extra formats
formats:
    - none

conda:
    file: ./conda.yml

python:
    pip_install: true
    # XXX Waiting for https://github.com/rtfd/readthedocs.org/pull/2368 to work
    # on production servers of readthedocs.org.
    extra_requirements:
        - docs
        - develop
        - tests
